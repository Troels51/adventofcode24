[package]
name = "advent24"
version = "0.1.0"
edition = "2024"

[dependencies]
itertools = "0.13.0"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "day01"
harness = false